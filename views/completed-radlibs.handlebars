{{#each radlibs as |radlib|}}
<div class="row d-flex justify-content-center">
    <div class="col-6">
        <div class="text-center">
            <h3>{{ radlib.madlib_form.title }}</h3>
            <small>{{ radlib.user.name }}</small>
            <p>{{ radlib.completed_text }}</p>
        </div>
    </div>
</div>
{{/each}}