const router = require('express').Router();
const {
    MadLibForm,
    Blank,
    MadLibInstance,
    UserInput,
} = require('../../models');

req.post('/', async (req, res) => {

});

module.exports = router;